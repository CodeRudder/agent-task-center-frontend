# V5.1 任务管理模块

## 概述

V5.1 任务管理模块为OpenClaw前端应用添加了完整的任务管理功能，支持任务的创建、编辑、查看、删除、筛选、排序等操作。

## 功能特性

### 1. 任务列表页面 (TaskListPage)
- **任务展示**：支持表格视图（桌面端）和卡片视图（移动端）
- **搜索功能**：按任务标题搜索
- **筛选功能**：按状态、优先级筛选
- **排序功能**：按标题、创建时间、截止日期排序
- **分页功能**：支持分页浏览
- **批量操作**：批量删除、批量归档、批量分配任务
- **快捷操作**：快速创建、编辑、删除任务

### 2. 任务创建 (TaskCreateModal)
- **基本信息**：任务标题、描述
- **状态选择**：待办、进行中、已完成、已取消
- **优先级选择**：低、中、高、紧急
- **负责人选择**：从Agent列表选择
- **截止日期**：日期时间选择器
- **标签管理**：多选标签
- **附件上传**：支持多文件上传

### 3. 任务编辑 (TaskEditModal)
- 复用任务创建页面的所有功能
- 加载现有任务数据
- 支持任务状态流转
- 显示现有附件列表

### 4. 任务详情 (TaskDetailPage)
- **任务基本信息展示**：标题、状态、优先级
- **任务描述展示**：完整描述内容
- **任务信息**：创建时间、截止日期、负责人、完成时间
- **任务评论功能**：查看和发表评论
- **任务附件列表**：查看和下载附件
- **任务历史时间线**：查看任务状态变更历史

## 技术架构

### 组件结构
```
src/
├── components/
│   ├── TaskCreateModal.tsx      # 任务创建模态框
│   ├── TaskEditModal.tsx        # 任务编辑模态框
│   ├── Select.tsx               # 选择框组件（新增）
│   ├── Textarea.tsx             # 文本域组件（新增）
│   ├── Button.tsx               # 按钮组件（复用V5.0）
│   ├── Input.tsx                # 输入框组件（复用V5.0）
│   ├── Modal.tsx                # 模态框组件（复用V5.0）
│   └── StatusBadge.tsx          # 状态徽章组件（复用V5.0）
├── pages/
│   ├── TaskListPage.tsx         # 任务列表页面
│   └── TaskDetailPage.tsx       # 任务详情页面
├── stores/
│   └── taskStore.ts             # 任务状态管理
├── services/
│   └── taskService.ts           # 任务API服务
└── types/
    └── task.ts                  # 任务类型定义
```

### 状态管理
使用Zustand创建taskStore，管理以下状态：
- `tasks`: 任务列表
- `selectedTasks`: 选中的任务ID数组
- `currentTask`: 当前任务详情
- `comments`: 任务评论列表
- `attachments`: 任务附件列表
- `history`: 任务历史记录
- `statistics`: 任务统计信息
- `pagination`: 分页信息
- `filters`: 筛选条件
- `sorting`: 排序条件
- `isLoading`: 加载状态
- `error`: 错误信息

### API集成
TaskService封装了所有任务相关的API调用：
- `getTasks`: 获取任务列表（支持筛选、排序、分页）
- `getTask`: 获取任务详情
- `createTask`: 创建任务
- `updateTask`: 更新任务
- `deleteTask`: 删除任务
- `batchDeleteTasks`: 批量删除任务
- `batchArchiveTasks`: 批量归档任务
- `batchAssignTasks`: 批量分配任务
- `getTaskComments`: 获取任务评论
- `createTaskComment`: 创建任务评论
- `deleteTaskComment`: 删除任务评论
- `getTaskAttachments`: 获取任务附件
- `uploadTaskAttachment`: 上传任务附件
- `deleteTaskAttachment`: 删除任务附件
- `getTaskHistory`: 获取任务历史
- `getTaskStatistics`: 获取任务统计

### 路由配置
- `/tasks`: 任务列表页面
- `/tasks/:id`: 任务详情页面
- 所有路由都需要登录才能访问

## 设计规范

### UI设计
- 参考V5.0的设计风格（颜色、间距、字体）
- 响应式设计（桌面端、平板端、移动端）
- 使用Tailwind CSS进行样式管理

### 颜色方案
- **主色**：蓝色 (blue-500)
- **成功**：绿色 (green-500)
- **警告**：黄色 (yellow-500)
- **危险**：红色 (red-500)
- **信息**：蓝色 (blue-500)

### 状态颜色
- **待办**：灰色 (gray)
- **进行中**：蓝色 (blue)
- **已完成**：绿色 (green)
- **已取消**：红色 (red)

### 优先级颜色
- **低**：灰色 (gray)
- **中**：蓝色 (blue)
- **高**：橙色 (orange)
- **紧急**：红色 (red)

## 使用说明

### 启动项目
```bash
cd /home/gongdewei/.openclaw/workspace-dev2/v5-frontend
npm run dev
```

### 访问任务管理
1. 登录系统
2. 访问 `/tasks` 查看任务列表
3. 点击"新建任务"创建任务
4. 点击任务行查看详情
5. 使用操作按钮编辑或删除任务

### 筛选和排序
1. 使用搜索框按标题搜索
2. 使用状态下拉框筛选任务状态
3. 使用优先级下拉框筛选任务优先级
4. 点击表头排序（标题、截止日期、创建时间）

### 批量操作
1. 勾选任务复选框
2. 使用批量删除、批量归档按钮

## 依赖项

新增依赖：
- 无（使用现有依赖）

现有依赖复用：
- react ^18.2.0
- react-dom ^18.2.0
- react-router-dom ^6.22.0
- zustand ^4.5.0
- axios ^1.6.7
- lucide-react ^0.344.0
- date-fns ^3.3.1
- tailwindcss ^3.4.1

## 后续优化建议

1. **性能优化**
   - 虚拟滚动（大量任务时）
   - 列表缓存
   - 图片懒加载

2. **功能增强**
   - 拖拽排序
   - 任务拖拽到日历
   - 任务模板
   - 任务提醒
   - 任务依赖关系

3. **用户体验**
   - 快捷键支持
   - 批量编辑
   - 导出功能
   - 打印功能

4. **移动端优化**
   - 更多手势操作
   - 更好的触摸体验

## 开发时间
预计：2-3小时
实际：已完成

## 开发者
Frontend Developer (Coding Agent)

## 版本
V5.1.0
